# 獨立審查報告：將專案治理規範同步至 GitHub 共享

- 審查對象 commit／版本：`e81aba6c06013664d771380bc6734a61d6e8fc80` 後續未提交 validator 修正／0.46.0
- 對應 08-CHANGE-LOG 條目：2026-07-23 — 將專案治理規範同步至 GitHub 共享
- 審查輪次：round2
- 審查代理啟動時間、上下文來源：2026-07-23 15:30 CST；同一獨立審查角色的新一輪上下文，輸入為 round1 後 `docs:check:final` 發現的兩項 validator 相容問題、主要代理修正摘要及指定唯一路徑；未修改 round1。

## 1. 需求完整性

- 判定：部分通過
- 證據：`scripts/project-docs-validator.mjs:26` 已讓「可逐字引用的完整結論句」與 round1 實際使用的「可逐字引用完整結論句」兩種格式都能解析；`scripts/test-project-docs-validator.mjs:8` 新增無「的」格式正例。`scripts/project-docs-validator.mjs:85-86` 也把 `提交／推送／共享` 納入外部發布動作，但目前只驗證核准範圍分別包含任一同意詞與任一動作詞，尚未驗證同意的受詞確實是該外部動作，未完整達成「明確同意／核准／接受提交、推送或共享」。

## 2. 邏輯正確性

- 判定：不通過
- 證據：`scripts/project-docs-validator.mjs:84-86` 先移除標點，再以兩個互相獨立的 regex 判斷 `同意|核准|接受` 與 `打包|發布|提交|推送|共享` 是否存在。這會接受「同意記錄需求；使用者要求推送治理資料至 GitHub 共享」：正向詞「同意」修飾的是記錄需求，外部動作仍只是「使用者要求」，但正規化後同時含 `同意`、`推送`、`共享` 且不含四個既有否定詞，因而不會產生「發布授權範圍必須明確記錄」錯誤。這與治理規則禁止以需求方要求某動作推定風險／發布授權的原則衝突。
- round1 標題相容修正本身正確：`(?:的)?` 只允許指定虛詞可選，仍要求完整固定欄位名稱、粗體內容與至少 20 字；針對性探針確認原 round1 報告可通過，而把欄位改為「可供引用結論」仍會回報缺少可逐字引用結論。

## 3. 邊界情況

- 判定：不通過
- 證據：2026-07-23 15:30 CST 執行既有與獨立授權矩陣：
  - 正向「產品負責人同意提交並推送治理資料至 GitHub 共享」「產品負責人核准推送治理資料」可通過。
  - 負向「使用者要求推送治理資料」「治理資料已推送共享」「未核准推送治理資料」「不同意共享治理資料」「接受提交但拒絕推送」均會被既有規則拒絕，顯示原有籠統需求、既成事實、否定及混合拒絕防線未回歸。
  - 未覆蓋且會誤收的語意分離案例為「同意記錄需求；使用者要求推送治理資料至 GitHub 共享」。新增 fixture 目前只有直接正向案例（`scripts/test-project-docs-validator.mjs:24`），沒有這類「無關同意＋籠統外部動作」負例。

## 4. 程式碼品質

- 判定：部分通過
- 證據：差異僅涉及 `docs/project-management/08-CHANGE-LOG.md`、`scripts/project-docs-validator.mjs` 與 `scripts/test-project-docs-validator.mjs`，範圍集中且 `git diff --check` exit 0。標題 regex 修改最小且可讀；外部動作清單與錯誤訊息同步更新。但授權 validator 使用不具語意關聯的關鍵字共現判斷，容易在新增動詞後擴大誤接受面，應將核准詞與動作詞綁定在同一正向授權片段，或明確拒絕「同意無關事項＋要求／請求外部動作」。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：2026-07-23 15:30 CST 執行 `node scripts/test-project-docs-validator.mjs` 通過；`npm run docs:check` 通過，驗證 19 個治理文件。sandbox 內第一次 `npm run check` 執行到 core 回歸時因 `listen EPERM 127.0.0.1:21715` 中止；依流程在允許本機監聽的環境以未修改原指令重跑，完整 `npm run check` exit 0，preflight、standing authorization、治理 validator、媒體、雙語字幕、AI、provider、review UI 與 core API 全部通過。
- 缺口：新增測試只覆蓋「直接明確同意 GitHub 提交／推送／共享」正例，未加入語意分離負例，因此完整回歸雖通過仍未捕捉上述 false positive。

## 6. 實際運行結果

- 判定：部分通過
- 證據：原 round1 報告實際使用「可逐字引用完整結論句」，修正後 `validateReviewReport` 能接受；移除該固定欄位名稱的負向探針仍被拒絕。實際執行的授權正負矩陣確認既有直接正例與五類負例結果符合預期，但依 `scripts/project-docs-validator.mjs:84-86` 的可執行判斷可確認「無關同意＋使用者要求推送／共享」仍符合所有接受條件。因這是發布授權治理門檻的 false positive，不能以其他完整測試通過取代。

## 綜合判定

- 結論：不通過
- 可逐字引用的完整結論句：**本輪 GitHub 治理同步 round2 獨立審查結論為不通過：round1 的「可逐字引用完整結論句」同義標題已能正確接受，既有籠統需求、否定與混合拒絕案例也未回歸，但 GitHub 外部共享授權仍只以同意詞與提交／推送／共享動作詞共現判斷，會誤接受「同意記錄需求；使用者要求推送治理資料」這類同意受詞無關的案例，因此明確核准外部動作的治理門檻與負向測試覆蓋尚未完成。**
- 阻擋問題（若有）：發布授權 validator 必須把 `同意／核准／接受` 的語意綁定到 `提交／推送／共享` 外部動作，並新增「無關同意＋使用者要求／請求推送或共享」負向 fixture；修正後需 round3 複審。
- 剩餘風險：關鍵字規則仍不是完整自然語言理解，即使修正鄰接或片段關聯，也應保留明示格式並以具代表性的語序、標點、否定與混合句 fixture 防止繞過。Draft PR 未建立及 secrets 掃描非完備等 round1 風險不因本輪 validator 修正而消失。
- 給主要開發代理的具體修正要求（若有）：
  1. 採正向授權片段格式，要求核准詞直接支配或緊鄰外部動作；不得只在整句任意位置各出現一次。
  2. 新增至少「同意記錄需求；使用者要求推送治理資料」「核准保存文件；需求方請求共享至 GitHub」負向 fixture，並保留直接正向、整句否定與部分接受／部分拒絕案例。
  3. 重跑治理 fixture、`npm run docs:check`、完整 `npm run check` 與 `git diff --check` 後進行 round3。

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
