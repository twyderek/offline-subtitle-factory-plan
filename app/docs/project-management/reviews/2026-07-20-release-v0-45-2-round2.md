# 獨立審查報告：發布多語言 LLM v0.45.2

- 審查對象 commit／版本：`168abd74656acd5f190c6e7576640ba86feac488`／`0.45.2` 發布候選，加上 round1 後的狀態文件與 `latest-mac.yml` 修正
- 對應 08-CHANGE-LOG 條目：`2026-07-20 — 發布多語言 LLM v0.45.2`
- 審查輪次：round2
- 審查代理啟動時間、上下文來源：2026-07-20（Asia/Taipei）；同一獨立審查角色的新複審回合，輸入為 round1 的兩項阻擋與修正後工作樹／候選資產；判定以本輪重新讀取、計算及只讀驗證為準。
- 審查環境：macOS／Node.js `v22.22.3`；候選資產 `/Users/nycu/Documents/離線字幕工廠/dist`；Windows 原始 artifact `/private/tmp/osf-v0452-win-artifact`。

## 1. 需求完整性

- 判定：通過
- 證據：
  - `docs/project-management/02-REQUIREMENTS-ANALYSIS.md:24-29,39-47` 的 FR-008、FR-009、FR-013 與 NFR-008 要求多語言標準化、cue 保護，以及版本、簽章、SHA、說明與資產一致；候選 commit、開發 round3 報告與 round1 封裝驗證已提供完整追溯。
  - round1 阻擋 1 已解除：`docs/project-management/00-CURRENT-STATUS.md:23-38` 現在區分 v0.45.1 現行公開 Release 與 v0.45.2 候選，逐項列出 Windows／macOS ASCII 候選資產，明示候選封裝完成但乾淨實機安裝／啟動未完成，並把 Windows 未簽章、macOS ad-hoc／未公證明確歸屬 v0.45.2。
  - `RELEASE-NOTES-0.45.2.md:3,30-39` 同樣明示 Windows 未使用 Authenticode、macOS 未使用 Apple Developer ID 簽章與公證、Windows／macOS 尚未完成乾淨實機測試。
  - `docs/project-management/08-CHANGE-LOG.md` 最新發布條目記錄需求提出者／產品負責人的具體授權，核准範圍逐項接受 Windows 未簽章、macOS 未公證及未完成跨平台乾淨實機測試；不是以單純「要求發布」替代風險接受。

## 2. 邏輯正確性

- 判定：通過
- 證據：
  - round1 阻擋 2 已解除：`/Users/nycu/Documents/離線字幕工廠/dist/latest-mac.yml` 的兩個 URL 現為 `offline-subtitle-factory-0.45.2-macos-arm64.zip` 與 `.dmg`，top-level path 亦為同一 ZIP；兩檔均實際存在且名稱完全一致。
  - 2026-07-20 13:26 CST 獨立計算：ZIP size `229385656`、SHA-512 Base64 `3X3qC1BQLEw34+nEUgcSyKHfMV/u8pHjbS8qbH0wd3grOwdxFyI6HyPxuPMBWabP0zhPDfTCuuRdPuuxMM/5HA==`；DMG size `223986517`、SHA-512 Base64 `VfpCDdD4jEv93MxOAbwhiBBcCjb/0muvXzIwlfDQNxLFiBvutn3iIqgsADqwPUJ0CPuyS8RWP4EpMyJ6vXCObQ==`，逐字等於 metadata。
  - Windows `/Users/nycu/Documents/離線字幕工廠/dist/latest.yml` 仍為 version `0.45.2`，URL／path 均指向實際 ASCII Setup `offline-subtitle-factory-setup-0.45.2.exe`，size `216812530`；round1 已確認其 SHA-512 與原始 artifact 相同。
  - package／lockfile、macOS App `CFBundleShortVersionString`／`CFBundleVersion` 均為 `0.45.2`，來源與候選封裝版本一致。

## 3. 邊界情況

- 判定：通過
- 證據：
  - 2026-07-20 13:26 CST 重新計算兩個 macOS 資產的 SHA-256，DMG `1ccafaf640a4f2194ce51ce0b797f5077003227f73426bf9e68476214d459304`、ZIP `cf2e72ac5e047c72540e24a189ac75c6f83fa338bdfe468224fd0dcccd3d35c6`，與 `SHA256SUMS-macos-arm64.txt` 一致；同時以實際 byte size 及 SHA-512 驗證 updater metadata，並非只比對檔名。
  - 本輪重跑 `hdiutil verify` 結果 VALID、`unzip -t` 無錯誤；Windows ASCII Setup／Portable 重新以 bundled `7za t` 驗證皆為 `Everything is Ok`（NSIS 尾端資料仍有既知 warning，不影響 archive 解讀）。
  - macOS `codesign --verify --deep --strict` 通過，但 `codesign -dv` 明示 `Signature=adhoc`、`TeamIdentifier=not set`；Windows `SIGNING-STATUS-windows-x64.txt` 仍明載 UNSIGNED，沒有將成功封裝誤稱為正式簽章。
  - 未覆蓋的 Windows／macOS 乾淨實機安裝、啟動、解除安裝與完整操作，以及真實供應商 LLM 語言遵循，均已在狀態及 Release notes 明示；前三項發布風險已有需求方具體接受。

## 4. 程式碼品質

- 判定：通過
- 證據：
  - 本機 HEAD 仍為候選 commit `168abd74656acd5f190c6e7576640ba86feac488`；`git diff --name-only` 顯示 round1 後 repo 既有修改僅為 `docs/project-management/00-CURRENT-STATUS.md` 與 `08-CHANGE-LOG.md`，沒有產品程式碼或測試變更。
  - `git diff --check` 於本輪通過；`npm run docs:check` 驗證 18 個治理文件與 0.45.2 版本通過。
  - 狀態修正保留「公開版仍為 0.45.1」與「0.45.2 僅為候選」的發布前語意，沒有把候選建置誤寫成已發布或實機驗收。
  - `latest-mac.yml` 的修正只調整 URL／path 至實際 ASCII 發布名稱，未改動已由檔案內容重新計算確認的 size、SHA-512 或 releaseDate，修改範圍與 round1 問題相符。

## 5. 測試覆蓋

- 判定：通過
- 證據：
  - round1 於 2026-07-20 13:20 CST 已在 sandbox 外完整執行 `npm run check` 並通過治理、語法、媒體、AI optimizer/provider、review UI 與核心本機 HTTP API；Windows Actions run `29716922238` 的候選 SHA、完整回歸、runtime、unsigned build、archive／SHA 與 artifact 上傳亦均成功。
  - 本輪嘗試再次於 sandbox 外執行 `npm run check`，但執行環境因用量限制拒絕建立程序；本代理未規避限制。由於 round1 後沒有程式碼／測試變更，本輪改以 `npm run docs:check`、`git diff --check`、狀態差異、metadata filename／size／SHA-512、兩平台 archive、macOS codesign／DMG／ZIP 做針對性複驗，全部通過。
  - round1 的 Windows Setup／Portable SHA-256 與原始 runner artifact 一致、Portable 含語言模組／0.45.2 手冊／動畫／runtime manifest／模型等證據仍適用；本輪重新測試兩個 Windows archive，未見檔案損壞。
  - 尚未涵蓋的乾淨實機與正式簽章／公證路徑是已揭露且授權接受的剩餘風險，不再是未揭露的測試缺口。

## 6. 實際運行結果

- 判定：通過
- 證據：
  - 本輪實際讀取修正後 `00-CURRENT-STATUS.md`，其公開版／候選版、跨平台封裝完成、三項主要發布風險與後續實機 smoke test 敘述互相一致，round1 的狀態矛盾已消失。
  - 本輪實際以候選 ZIP／DMG bytes 重新計算 SHA-512 及 size，均與修正後 `latest-mac.yml` 完全一致；Windows `latest.yml` 的 ASCII Setup 名稱亦與實際候選檔一致。
  - macOS App、DMG、ZIP 與兩個 Windows EXE archive 均可由只讀工具成功驗證；版本與簽章狀態沒有不實宣稱。
  - tag／GitHub Release 尚未建立仍是預期發布前狀態；本結論表示候選可進入建立 tag／Release 與上傳步驟，不表示發布後 digest、下載 URL 或 GitHub 實際資產名稱已驗證。那些項目必須在發布後另行核對才能結案。

## 綜合判定

- 結論：通過
- 可逐字引用的完整結論句：**本輪 round2 獨立發布審查結論為通過：round1 的狀態文件矛盾與 macOS updater 檔名不一致均已解除，00-CURRENT-STATUS 現在準確區分 v0.45.1 公開版與 v0.45.2 候選並明示 Windows 未簽章、macOS 未公證及未完成跨平台乾淨實機測試，latest-mac.yml 的 ASCII ZIP／DMG 名稱、實際大小與重新計算的 SHA-512 亦完全一致；結合候選 commit、Windows CI、完整回歸、版本、封裝、SHA 與簽章狀態證據，發布前六面向未發現未處理阻擋問題。**
- 阻擋問題（若有）：無。
- 剩餘風險：Windows Authenticode、Apple Developer ID／notarization、跨平台乾淨實機完整操作及真實供應商 LLM 語言遵循仍未覆蓋；前三項已有需求方具體接受，仍須在最終 Release、狀態與交付持續揭露。本輪因執行環境用量限制未能再次啟動 sandbox 外完整核心 HTTP 回歸，但 round1 的同日完整通過證據仍有效，且 round1 後沒有產品程式碼／測試變更。GitHub Actions UI 顯示的 workflow 名稱曾為 0.45.1，發布證據應繼續以 run ID、head SHA、job 與候選 workflow 檔案共同標示。
- 給主要開發代理的具體修正要求（若有）：無發布前阻擋修正。可建立 `v0.45.2` tag／Release 並上傳已核對的 ASCII 資產；發布後必須重新核對 GitHub 實際資產名稱、大小、digest、checksum 內容、updater metadata 與直接下載 URL，再更新公開版本及結案文件。不得把本報告解讀為已完成發布後驗證。

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
