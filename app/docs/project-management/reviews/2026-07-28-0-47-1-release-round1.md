# 獨立審查報告：0.47.1 修正版發布

- 審查對象 commit／版本：`0bd3b53be0cd523d7c7beb3078b5b46dad2f81b1`／`0.47.1`；tag `v0.47.1`
- 對應 08-CHANGE-LOG 條目：2026-07-28 — 0.47.1 修正版發布
- 審查輪次：round1
- 審查代理啟動時間、上下文來源：2026-07-28（Asia/Taipei）；獨立上下文，未沿用開發代理對話記憶；僅讀取、執行與驗證。

## 審查範圍與外部可達性

本輪先以本地 Git、治理文件、Release notes、既有工作紀錄及本地產物核對；再以 GitHub REST API 查詢 `https://api.github.com/repos/twyderek/offline-subtitle-factory-app/releases/tags/v0.47.1`。2026-07-28 執行 `curl -sS` 回覆 `curl: (6) Could not resolve host: api.github.com`，因此本輪沒有可獨立引用的 GitHub Release JSON、7 項 asset 的實際大小／digest／browser_download_url，亦沒有發布後下載回應。這不是把工作紀錄中的宣稱當成遠端證據。

## 1. 需求完整性

- 判定：部分通過
- 證據：本地 `git rev-parse HEAD`、`git rev-parse v0.47.1^{commit}` 與 `git show -s --format='%H %s' 0bd3b53` 均指向 `0bd3b53be0cd523d7c7beb3078b5b46dad2f81b1`；`.github/workflows/windows-preview.yml` 已改為 0.47.1；`RELEASE-NOTES-0.47.1.md` 已存在並揭露 rule-score、Metal exit 139、未簽章／未公證與實機缺口。`00-CURRENT-STATUS.md` 與 `08-CHANGE-LOG.md` 聲稱 GitHub 已公開 7 項資產，但因 API/DNS 失敗無法獨立證實 repository、Release target、asset 清單、大小、digest、URL 或下載結果。

## 2. 邏輯正確性

- 判定：部分通過
- 證據：tag 與本地 commit 關係一致，且工作紀錄保留 v0.47.0 舊 target，不要求移動舊 tag；發布 notes 的限制敘述與狀態文件揭露相容。另一方面，`00-CURRENT-STATUS.md` 同時保留早期「現行公開 Release 已為 v0.45.2」段落，又新增「0.47.1 已公開發布」段落，文件內部存在歷史敘述未完全收斂的歧義；需要發布後遠端證據及文件結案修正才能判定完整正確。

## 3. 邊界情況

- 判定：部分通過
- 證據：`RELEASE-NOTES-0.47.1.md` 明確列出 Metal exit 139、缺少 confidence 時的 rule-score fallback、Windows 未 Authenticode、macOS 未 Developer ID／公證、Electron／跨平台實機與長音訊轉錄缺口；`00-CURRENT-STATUS.md` 亦保留相同風險。未能取得 GitHub asset 下載，故無法檢查七個公開檔名是否 ASCII、`latest.yml` URL／size 是否指向實檔、checksum 是否涵蓋正確 Windows 檔案，亦無法驗證下載失敗／重試邊界。

## 4. 程式碼品質

- 判定：通過（就本次發布來源變更範圍）
- 證據：`git show --stat 0bd3b53` 顯示變更集中於版本／Release notes、Windows workflow、治理紀錄與 package lock；未見本輪審查需要指出的秘密、憑證或不相關大型程式碼改動。`git status --short` 顯示既有 `docs/project-management/00-CURRENT-STATUS.md` 修改及未追蹤歷史審查檔，均屬審查前既有工作樹狀態，本輪未修改或清理。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：工作紀錄與狀態文件記載 `npm run check`、Windows CI、Windows artifact 解壓／SHA、macOS `hdiutil verify`／`unzip -t` 已執行；本輪可重現的本地檢查中，`unzip -t ../dist/offline-subtitle-factory-0.47.1-macos-arm64.zip` 失敗（`End-of-central-directory signature not found`），該 8.0 MB 檔案看起來是不完整或非 ZIP 下載，故不能將本地檔案當成成功的 macOS ZIP 證據。由於 GitHub API/DNS 不可達，七項公開 asset 的發布後驗證與 Windows artifact 解壓內容無法在本輪重現。

## 6. 實際運行結果

- 判定：不通過（證據不足且出現本地產物失敗）
- 證據：本地可驗證 tag／commit；但 GitHub API 查詢在 2026-07-28 失敗於 DNS，未取得 Release target 或下載回應。可見的 `../dist/offline-subtitle-factory-0.47.1-macos-arm64.zip` 執行 `unzip -t` 失敗，不能證明可下載／可安裝。沒有本輪可追溯的 Windows Setup／Portable 解壓清單、實機安裝啟動、Electron renderer 或真實轉錄結果。

## 發布等級額外核對

- 發布授權：`08-CHANGE-LOG.md` 記錄需求提出者／產品負責人於 2026-07-28 明確同意建立並發布 0.47.1，並引用 `AUTH-2026-07-23-01` 接受 Windows 未簽章與 macOS 未公證風險；授權同時明確排除資產／checksum／metadata 不一致、未完成測試或審查失敗下的發布。
- Release notes／狀態揭露：未簽章、未公證、Metal、Electron、跨平台實機與長音訊缺口均有揭露；但 `00-CURRENT-STATUS.md` 尚殘留舊版「現行公開 Release 已為 v0.45.2」敘述，需結案時收斂。
- 7 項 asset 預期集合（由工作紀錄／狀態文件可推得）：macOS arm64 DMG、macOS arm64 ZIP、macOS SHA 清單、Windows Setup、Windows Portable、`latest.yml`、Windows SHA 清單。實際名稱、大小、GitHub digest、直接 URL、下載後 SHA：**待確認**，因 API/DNS 失敗；不得以工作紀錄宣稱替代證據。
- Windows artifact 解壓證據：工作紀錄有宣稱已核對，但本輪沒有可讀 artifact 檔案或可重現命令輸出；**待確認**。
- macOS `hdiutil verify`／`unzip -t`：既有工作紀錄宣稱通過，但本輪唯一可見 0.47.1 ZIP 實測失敗；DMG 檔案不可見，因此本輪不能獨立確認。

## 綜合判定

- 結論：不通過
- 阻擋問題：
  1. GitHub API／下載 DNS 失敗，無法獨立核對 Release repository／target、7 項 asset 名稱／大小／digest／URL 或發布後下載。
  2. 本地可見 0.47.1 macOS ZIP `unzip -t` 失敗，且無可重現的 0.47.1 DMG 或 Windows artifact 解壓證據。
  3. `00-CURRENT-STATUS.md` 同時保留 v0.45.2 現行公開版本敘述與 v0.47.1 已公開敘述，結案狀態未完全一致。
- 剩餘風險：Windows 未 Authenticode、macOS 未 Developer ID／公證、Metal exit 139、Electron／跨平台實機與真實長音訊轉錄仍未驗收；這些風險雖由常設授權涵蓋簽章／公證部分，但不涵蓋未驗證資產或測試。
- 給主要開發代理的具體修正要求：網路恢復後重新取得 GitHub API JSON，逐項記錄 7 個 asset 的實際 name／size／digest／browser_download_url；下載每項並核對 checksum；重新取得完整 macOS DMG／ZIP 與 Windows artifact，重跑 `hdiutil verify`、`unzip -t`、Windows 解壓／機密排除與 metadata URL／size；修正狀態文件舊版現行版本敘述；完成受影響測試後建立 round2 審查，未完成前不得宣稱發布通過。

- 可逐字引用完整結論句：**本輪 round1 獨立發布審查結論為不通過：雖然本地 `v0.47.1` tag 已解析至 `0bd3b53` 且發布風險有所揭露，但 GitHub API／下載受 DNS 失敗阻擋、7 項公開資產的名稱／大小／digest／URL 與 Windows artifact 解壓證據無法獨立核對，且可見 0.47.1 macOS ZIP 的 `unzip -t` 失敗，因此不得宣稱 v0.47.1 發布通過。**

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
