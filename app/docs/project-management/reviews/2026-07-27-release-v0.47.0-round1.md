# 0.47.0 最終發布獨立審查 round1

- 審查日期：2026-07-27（Asia/Taipei）
- 審查範圍：`/Users/nycu/Documents/離線字幕工廠/offline-subtitle-factory-app`
- 審查方式：只讀證據檢查；本輪嘗試交由獨立審查上下文執行，但該上下文未在本次回報時限內返回，因此本文件是阻擋性審查報告，不是發布通過核准。
- 資料截止：本輪已取得的本機與 GitHub Actions 證據，以及先前建立 GitHub Release 的操作結果。

## 完整單句結論

0.47.0 目前不可被認定為完成最終發布審查：版本與分支內容、macOS arm64 資產及 SHA、Windows CI run 30231912997 的成功結果均有可核對證據，但目前遠端分支／本機 HEAD 為 `7517dc3`、Windows CI 與既有 GitHub Release 建立目標為 `7946f7f`，本輪未取得 GitHub Release 最新資產清單與發布後下載核對，且獨立審查上下文未完成回報，因此在 Release 目標一致性、資產發布核對及獨立審查完成前不得宣稱最終發布通過；未簽章、未公證、Metal exit 139 與跨平台／Electron 實機驗收不足則是已揭露但仍須保留的條件風險。

## 阻擋問題

1. **Release 目標與目前分支不一致**：本機與 `origin/codex/release-v0.47.0` 的 HEAD 是 `7517dc35adbb1105e81e76f725958da15dc656ca`，而已取得的 Windows CI artifact 與先前建立 `v0.47.0` Release 的目標是 `7946f7fa8e080f28a65639053f674fa8babcd5fe`；差異包含 `RELEASE-SHA256SUMS-0.47.0-macos-arm64.txt` 與發布收尾紀錄，尚未證明 Release 代表目前完整定版內容。
2. **GitHub Release 資產與發布後下載未完成核對**：已知 Release URL 為 `https://github.com/twyderek/offline-subtitle-factory-app/releases/tag/v0.47.0`，但本輪 GitHub CLI 因網路解析失敗，GitHub connector 也沒有可讀 Release 資產清單的工具；因此不能確認 macOS DMG／ZIP／SHA 檔是否已上傳、公開下載內容是否與本機資產相同。
3. **獨立審查未完成**：本輪獨立審查上下文呼叫未在回報時限內返回，指定報告檔原先不存在；本報告由主上下文依已取得證據寫入，不能替代治理規範要求的獨立審查代理簽核。

## 六面向審查

### 1. 需求／治理

- 版本 `package.json` 為 `0.47.0`。
- 分支為 `codex/release-v0.47.0`，且本機與 `origin/codex/release-v0.47.0` 同指 `7517dc3`。
- `RELEASE-NOTES-0.47.0.md` 明確揭露 rule-score fallback、Whisper engine quality 缺失、Metal exit 139、未簽章／未公證及跨平台／Electron 實機驗收不足。
- `docs/project-management/08-CHANGE-LOG.md` 的 0.47.0 條目記錄了發布授權與條件限制，但該條目所記錄的 Release 目標仍為 `7946f7f`，需與目前 `7517dc3` 收斂。
- 判定：**條件具備，因目標一致性與獨立發布審查未完成而阻擋**。

### 2. 實作／版本內容

- HEAD `7517dc3` 的提交訊息為 `release: record 0.47.0 verification evidence`。
- 相較 `7946f7f`，目前 HEAD 只新增 `RELEASE-SHA256SUMS-0.47.0-macos-arm64.txt` 並更新發布收尾紀錄，未見產品程式碼差異。
- Release notes 將 0.47.0 定位為低可信片段校閱與安全品質回落工作流，未宣稱不存在的 engine confidence。
- 判定：**版本內容可讀且限制揭露充分，但 Release 目前尚未證明指向完整 HEAD**。

### 3. 測試／CI

- 已記錄 `npm run check` 通過、`npm run runtime:verify:mac` 通過、macOS DMG `hdiutil verify` 通過、macOS ZIP `unzip -t` 通過。
- Windows CI run `30231912997` 的 job `89872267111` 為 completed／success；source tests、real FFmpeg regression tests、unsigned preview package build、archive／SHA verification、artifact upload 均成功。
- Windows workflow 的 Authenticode-signed NSIS Setup／Portable step 為 **skipped**，不是簽章成功。
- CI artifact 為 `offline-subtitle-factory-0.47.0-windows-x64`，artifact id `8640388049`，大小 `433249293` bytes，digest `sha256:d5bcf7dae361c11955e4d1c1054a09b6d2203280c064da89c1fc4a77d285c8d6`，head SHA 為 `7946f7f...`。
- 判定：**CI 建置成功，但成功只覆蓋 Windows unsigned preview 與自動化測試，不能替代安裝後、Electron renderer、跨平台或實機驗收**。

### 4. 資產／SHA

- 本機 macOS arm64 DMG：`dist/offline-subtitle-factory-0.47.0-macos-arm64.dmg`。
- 本機 macOS arm64 ZIP：`dist/offline-subtitle-factory-0.47.0-macos-arm64.zip`。
- 本機 SHA-256：
  - DMG：`d96d7a63a7793d221c9dea4acb113ef13d6f90f5d49c3466481096b2c501a6ee`
  - ZIP：`aebc1e533e28fe8910a61897cdad974295fb7bce507898c2da16701f83b2dec6`
- `RELEASE-SHA256SUMS-0.47.0-macos-arm64.txt` 逐項記錄上述兩個摘要，且檔案存在於目前 HEAD。
- 判定：**本機 macOS 資產與 SHA 內部一致；GitHub Release 上傳與下載後 digest 尚未證明**。

### 5. 發布一致性／可追溯性

- 先前已建立 GitHub Release `v0.47.0`，URL 為 `https://github.com/twyderek/offline-subtitle-factory-app/releases/tag/v0.47.0`。
- 已取得的建立結果指向 `7946f7f`；目前分支／遠端 HEAD 為 `7517dc3`，形成可追溯性缺口。
- Windows artifact 保留在成功的 Actions run，不能在未完成下載與內容核對前視為 GitHub Release 公開資產。
- 本輪未能確認 Release 是否已包含 macOS 資產、SHA 檔，亦未能完成公開 URL 下載後的 SHA 比對。
- 判定：**不通過，為發布阻擋面向**。

### 6. 安全／簽章／跨平台風險

- Windows 產物未完成 Authenticode 簽章；macOS 產物未完成 Apple Developer ID 簽章與公證。
- Apple Silicon Metal runtime smoke 曾發生 exit 139；CPU workaround 的成功不代表 Metal 或跨平台 runtime 驗收完成。
- Windows／macOS 安裝後、Electron renderer、Windows CI 安裝流程及真實長音訊轉錄仍缺少完整實機證據。
- Release notes 已揭露以上風險，並要求先核對 SHA-256、在非關鍵環境測試。
- 判定：**風險已揭露且可條件發布，但在未簽章／未公證狀態下不得當作一般使用者安全發布**。

## 可逐字引用的結論

> 0.47.0 目前不可被認定為完成最終發布審查：版本與分支內容、macOS arm64 資產及 SHA、Windows CI run 30231912997 的成功結果均有可核對證據，但目前遠端分支／本機 HEAD 為 `7517dc3`、Windows CI 與既有 GitHub Release 建立目標為 `7946f7f`，本輪未取得 GitHub Release 最新資產清單與發布後下載核對，且獨立審查上下文未完成回報，因此在 Release 目標一致性、資產發布核對及獨立審查完成前不得宣稱最終發布通過；未簽章、未公證、Metal exit 139 與跨平台／Electron 實機驗收不足則是已揭露但仍須保留的條件風險。

## 後續解除阻擋所需證據

- 讓 `v0.47.0` Release／tag、Windows artifact、Release notes、macOS 資產與目前定版 commit `7517dc3` 一致，或建立可明確追溯的新修正版版本，不得靜默重指既有 tag。
- 取得 GitHub Release 資產清單，下載每一項公開資產並比對 SHA／digest；補足 Windows 資產是否直接附於 Release 的明確紀錄。
- 由獨立審查代理完成本檔案的六面向獨立審查，並在不修改其他檔案的前提下重新給出通過或條件通過判定。
- 維持未簽章／未公證、Metal exit 139、跨平台／Electron 實機驗收不足與 Whisper engine quality 缺失的明確揭露。
