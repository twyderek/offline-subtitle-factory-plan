# 獨立審查報告：0.47.1 修正版發布

- 審查對象 commit／版本：`0bd3b53be0cd523d7c7beb3078b5b46dad2f81b1`／`0.47.1`；tag `v0.47.1`
- 對應 08-CHANGE-LOG 條目：2026-07-28 — 0.47.1 修正版發布
- 審查輪次：round3（不覆寫 round1／round2）
- 審查代理啟動時間、上下文來源：2026-07-28（Asia/Taipei）；獨立上下文；僅讀取、執行與驗證。

## 1. 需求完整性

- 判定：部分通過
- 證據：本地 `v0.47.1` tag 解析至 `0bd3b53be0cd523d7c7beb3078b5b46dad2f81b1`。主要代理提供的 `gh release view v0.47.1 --json` 輸出為 `isDraft=false` 且列出 7 項資產：`latest.yml`、macOS arm64 DMG／ZIP、Windows Setup／Portable、Windows SHA256 清單與 signing-status。GitHub API 在本審查上下文仍無法重放，故外部欄位沿用該可追溯輸出並標記待重放。

## 2. 邏輯正確性

- 判定：通過
- 證據：`latest.yml` 的 Setup size `216823277` 與 `/tmp/osf-0471/windows-extracted/offline-subtitle-factory-setup-0.47.1.exe` 一致；Windows SHA 清單與本地 SHA 一致。Release notes 明確揭露 unsigned／未公證及 Metal、Electron、實機缺口。

## 3. 邊界情況

- 判定：部分通過
- 證據：`hdiutil verify /tmp/osf-0471/offline-subtitle-factory-0.47.1-macos-arm64.dmg` exit 0、checksum VALID；`unzip -t /tmp/osf-0471/offline-subtitle-factory-0.47.1-macos-arm64.zip` exit 0、No errors detected；Windows artifact 展開為預期 6 檔。乾淨實機安裝、Metal exit 139 與真實長音訊仍未覆蓋。

## 4. 程式碼品質

- 判定：通過
- 證據：`0bd3b53` 的發布變更集中於版本、Release notes、workflow 與治理紀錄；本輪未修改產品程式碼或既有審查檔。`SIGNING-STATUS-windows-x64.txt` 明確標示 `UNSIGNED INTERNAL PREVIEW`，未誤宣稱簽章。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：本輪實際完成 DMG／ZIP 完整性、macOS SHA、Windows artifact 清單／大小／SHA／metadata 交叉核對；主要代理提供的 gh 輸出補足 Release `isDraft=false` 與 7 assets 的 name／size／digest／URL。API／下載在本上下文未能重放；未覆蓋乾淨實機與 Metal。

## 6. 實際運行結果

- 判定：部分通過
- 證據：本地 hdiutil、unzip、SHA 與 Windows 展開驗證均成功；公開 Release 狀態依主要代理 gh 輸出為非 draft。直接下載 HTTP 與 GitHub digest 在本上下文待網路恢復後重放，無實機安裝／轉錄證據。

## 發布等級額外核對

- 發布授權：`08-CHANGE-LOG.md` 記錄需求提出者於 2026-07-28 授權發布 0.47.1；引用 `AUTH-2026-07-23-01` 接受 Windows 未簽章、macOS 未 Developer ID／公證風險。授權不涵蓋資產／checksum 不一致或測試／審查失敗。
- 風險揭露：`RELEASE-NOTES-0.47.1.md` 與狀態文件揭露未簽章、未公證、Metal、Electron 與跨平台實機缺口；不得將其描述為已完成。

## 綜合判定

- 結論：有條件通過
- 阻擋問題（若有）：無；條件風險已揭露。
- 條件：網路恢復後重放 GitHub API／直接下載並記錄 7 assets 的完整欄位；持續揭露 unsigned／未公證／Metal／Electron／實機缺口；結案時收斂狀態文件殘留的舊版 v0.45.2 現行公開版本敘述。
- 剩餘風險：Windows 未 Authenticode、macOS 未 Developer ID／公證、Metal exit 139、Electron／跨平台實機與真實長音訊尚未驗收。
- 給主要開發代理的具體修正要求：完成上述 API／下載重放與狀態文件收斂；若核對結果改變，建立下一輪複審。

- 可逐字引用完整結論句：**本輪 round3 獨立發布複審結論為有條件通過：本地 `v0.47.1` tag／commit、macOS DMG／ZIP 完整性與 SHA、Windows artifact 展開內容／SHA／`latest.yml`，以及主要代理提供的 GitHub `isDraft=false`、7 項公開資產證據一致；在網路可達時重放 GitHub API／下載核對、收斂狀態文件，並持續揭露 Windows 未簽章、macOS 未公證、Metal／Electron／跨平台實機缺口的條件下，可以維持 v0.47.1 公開發布。**

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
