# 獨立審查報告：發布多語言 LLM v0.45.2

- 審查對象 commit／版本：`168abd74656acd5f190c6e7576640ba86feac488`／`0.45.2` 發布候選
- 對應 08-CHANGE-LOG 條目：`2026-07-20 — 發布多語言 LLM v0.45.2`
- 審查輪次：round1
- 審查代理啟動時間、上下文來源：2026-07-20（Asia/Taipei）；由主要開發代理啟動的獨立上下文，只以本輪讀取的 repo、GitHub 狀態、本機候選資產與 Windows 原始 artifact 為判定依據，未沿用開發代理對成果的評價。
- 審查環境：macOS／Node.js `v22.22.3`；候選資產 `/Users/nycu/Documents/離線字幕工廠/dist`；Windows 原始 artifact `/private/tmp/osf-v0452-win-artifact`。

## 1. 需求完整性

- 判定：部分通過
- 證據：
  - `docs/project-management/02-REQUIREMENTS-ANALYSIS.md:24-29,39-47` 的 FR-008、FR-009、FR-013 與 NFR-008 要求多語言標準化、cue 保護及版本／簽章／SHA／資產一致；候選 commit 包含 `lib/ai/languages.mjs`、UI、server、測試、0.45.2 手冊及 Release notes。
  - `RELEASE-NOTES-0.45.2.md:3,30-39` 明示 Windows 未使用 Authenticode、macOS 未使用 Apple Developer ID 簽章與公證、Windows／macOS 尚未完成乾淨實機安裝測試，符合風險揭露要求。
  - `docs/project-management/08-CHANGE-LOG.md:51,67-72` 記錄需求提出者／產品負責人的發布授權、時間與範圍，逐項涵蓋 Windows 未簽章、macOS 未公證及未完成跨平台乾淨實機測試。
  - 阻擋：`docs/project-management/00-CURRENT-STATUS.md:23-28,32-37` 仍列 0.45.1 資產，Windows 未簽章亦只明指 0.45.1，且第 37 行聲稱多語言版本尚未做 Windows／macOS 封裝；這與本輪已存在且驗證的 0.45.2 候選資產矛盾，未達「狀態如實揭露」及 NFR-008。

## 2. 邏輯正確性

- 判定：部分通過
- 證據：
  - `package.json:3,60,83-87,117-143`、`package-lock.json:3,9`、App `Info.plist` 的 `CFBundleShortVersionString`／`CFBundleVersion` 均為 `0.45.2`，封裝也指向 `docs/0.45.2`。
  - Windows 原始 `latest.yml` 與發布目錄 `latest.yml` 都指向 `offline-subtitle-factory-setup-0.45.2.exe`，size `216812530`、SHA-512 相同；Setup／Portable 改為 ASCII 名稱後 SHA-256 分別仍為 `9a61a013...67531`、`74f3a1c9...babae`，與 runner checksum 完全一致。
  - 阻擋：`/Users/nycu/Documents/離線字幕工廠/dist/latest-mac.yml` 指向 `offline-subtitle-factory-0.45.2-arm64-mac.zip` 與 `offline-subtitle-factory-0.45.2.dmg`，實際準備的 ASCII 資產卻是 `offline-subtitle-factory-0.45.2-macos-arm64.zip` 與 `.dmg`；若上傳此 updater metadata，下載路徑必然不一致。發布前須重寫並驗證 metadata，或明確排除 `latest-mac.yml` 並記錄本版不提供該自動更新 metadata。

## 3. 邊界情況

- 判定：部分通過
- 證據：
  - 2026-07-20 13:20 CST 重新執行 `npm run check`：sandbox 內核心測試因 `listen EPERM 127.0.0.1` 中止，經授權於 sandbox 外重跑後治理、媒體、AI optimizer/provider、review UI 與核心 API 全部通過；涵蓋 BCP 47、非法 API 值、舊設定回退、cue ID／數量／順序／時間碼保護。
  - Windows Setup 與 Portable 以 bundled `7za t` 均回報 `Everything is Ok`（NSIS 尾端資料產生既知 warning）；Portable 清單實際含 `resources/app/lib/ai/languages.mjs`、0.45.2 手冊、三段 `.osfvideo`、runtime manifest 與 `ggml-tiny.bin`。
  - Windows PE Security Directory 為 `00000000 00000000`，CI 的 signed build step skipped、unsigned build step success，且 artifact 的 `SIGNING-STATUS-windows-x64.txt` 明載 UNSIGNED。
  - 未覆蓋且已授權接受：Windows／macOS 乾淨實機安裝、啟動、解除安裝及完整操作；真實供應商 LLM 的語言遵循仍需人工確認。

## 4. 程式碼品質

- 判定：通過
- 證據：
  - 候選 commit `168abd74656acd5f190c6e7576640ba86feac488` 與本機 HEAD 完全相同；PR #6 的 head OID 亦相同，避免審查到不同來源。
  - 多語言核心集中於 `lib/ai/languages.mjs`，server、optimizer 與 UI 使用同一標準化邊界；開發 round3 報告已驗證 255／256 字元、注入與 cue 順序回歸，本輪完整測試未見回歸。
  - `git diff --check` 通過；既有未提交變更只有主要代理更新中的 `docs/project-management/08-CHANGE-LOG.md`，本代理未修改該檔。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：
  - 本輪 2026-07-20 13:20 CST 完整 `npm run check` 在 sandbox 外通過；Windows Actions run `29716922238` 的 head SHA 為候選 commit，job `88271922809` conclusion `success`，完整回歸、runtime 準備、unsigned build、archive／SHA 驗證與 artifact 上傳步驟均成功。
  - CI 顯示 signed build step skipped、unsigned build step success，和發布風險敘述一致；GitHub API 將 workflow 顯示名回報為 `Build Windows 0.45.1`，但候選檔案 `.github/workflows/windows-preview.yml:1,7-9,100` 已是 0.45.2，run 的 head SHA／branch 可追溯正確。
  - macOS App `codesign --verify --deep --strict` 通過，`codesign -dv` 明示 `Signature=adhoc`、`TeamIdentifier=not set`；DMG `hdiutil verify` 為 VALID，ZIP `unzip -t` 無錯誤，SHA-256 與 `SHA256SUMS-macos-arm64.txt` 一致。
  - 未覆蓋：兩平台乾淨實機測試、Windows Authenticode 成功路徑、Apple Developer ID／notarization；前三項發布風險已獲需求方明確接受，但不能消除狀態文件與 updater metadata 不一致的阻擋。

## 6. 實際運行結果

- 判定：部分通過
- 證據：
  - macOS 未封裝 App 的版本、深層 ad-hoc 簽章、DMG、ZIP、手冊與 runtime 可由本機實際驗證；四個最終 ASCII 候選資產大小分別為 DMG `223986517`、ZIP `229385656`、Setup `216812530`、Portable `216107423` bytes。
  - Windows 原始 artifact 與改名後發布候選 SHA-256 逐檔相同，Setup／Portable 可由 7-Zip 展開驗證且關鍵內容存在；但未在 Windows 乾淨實機安裝或啟動，僅能判定封裝／CI 結果，不得宣稱實機驗收。
  - GitHub PR #6 為 open draft、mergeable，head 指向候選 SHA；`v0.45.2` tag 與 Release 尚未建立，符合指定的預期發布前狀態，不構成本輪阻擋，也因此尚無發布後 digest／下載 URL 可驗證。
  - 實際發布仍被狀態文件矛盾與 `latest-mac.yml` 路徑不一致阻擋。

## 綜合判定

- 結論：不通過
- 可逐字引用的完整結論句：**本輪 round1 獨立發布審查結論為不通過：候選 commit、PR／Windows CI、版本、SHA、Windows latest.yml、封裝內容與未簽章／ad-hoc 簽章證據均可追溯，Release notes 與發布授權也完整接受並揭露 Windows 未簽章、macOS 未公證及未完成跨平台乾淨實機測試；但 00-CURRENT-STATUS 仍列 0.45.1 資產並錯稱多語言版本尚未跨平台封裝，且 latest-mac.yml 指向不存在的非發布檔名，因此發布狀態真實性與 updater 資產一致性尚有兩項未處理阻擋。**
- 阻擋問題（若有）：
  1. 更新 `docs/project-management/00-CURRENT-STATUS.md`，如實列出 0.45.2 已完成的候選封裝／SHA 狀態，並把 Windows 未簽章、macOS 未公證、未完成 Windows／macOS 乾淨實機測試明確歸屬於 0.45.2；仍須保留公開版為 0.45.1，直到 tag／Release 真正完成。
  2. 使 `latest-mac.yml` 的 URL／path 與實際 ASCII macOS ZIP／DMG 名稱一致並驗證 SHA-512／size；若本版不發布該 metadata，須從發布資產集合排除並在治理／發布紀錄明示。
- 剩餘風險：即使解除上述阻擋，Windows Authenticode、Apple Developer ID／notarization、跨平台乾淨實機完整操作及真實 LLM 語言遵循仍未覆蓋；前三項已有可查核的需求方風險接受，仍須在最終 Release、狀態與交付持續揭露。GitHub Actions 顯示的 workflow 名稱仍為 0.45.1，建議發布證據以 run ID、head SHA、job 與候選 workflow 檔案共同標示，避免誤讀。
- 給主要開發代理的具體修正要求（若有）：只修正上述兩項阻擋並重跑 `npm run check`、macOS metadata 的檔名／size／SHA-512 驗證及文件檢查；完成後建立 round2 報告複審，不得修改本報告。tag／Release 建立與上傳後仍須另做發布後資產名稱、大小、digest、checksum 與下載驗證。

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
