# 獨立審查報告：0.48 中文全形標點 preservation prompt

- 審查對象 commit／版本：`3e625c4`／`0.48.0`，分支 `codex/0.48-local-llm`。
- 對應工作：2026-07-29 — 本機 LLM 結構化輸出修正（中文全形標點品質補強）。
- 審查輪次：round10
- 審查代理啟動時間、上下文來源：2026-07-29（Asia/Taipei）；獨立讀取 commit diff、目前 Ollama／LM Studio 文件、round9 報告與測試，並以相同 native Ollama schema／prompt 重跑單 cue。

## 1. 需求完整性

- 判定：部分通過
- 證據：
  - `lib/ai/subtitle-optimizer.mjs:136` 新增明確要求保留中文全形標點，並保留 ID／順序／段落數／時間碼／實際文字／anti-placeholder 約束。
  - 變更只影響 prompt，不放寬 native JSON Schema 或 strict cue validation，也不自動套用未人工確認的建議。
  - 文件 `08-CHANGE-LOG.md:61` 如實記錄真實重測仍將「。」改為「.」，並保留人工品質風險；LM Studio structured output／真正斷網仍未完成。

## 2. 邏輯正確性

- 判定：通過（提示詞／安全邊界）
- 證據：
  - 本輪 prompt 要求與 schema、native Ollama transport、cue contract 串接一致，未改動 endpoint、Key、redirect 或資料傳輸政策。
  - 2026-07-29 直接以相同 native `/api/chat` schema、`temperature:0` 與全形標點 prompt 呼叫 `llama3.2:1b`，回應為一個合法 `cues` 陣列，id 1，未增加額外 cue。
  - 回應文字仍為 `這是一個測試字幕.`，顯示提示詞不能強迫此模型遵守全形標點；文件將其列為品質風險而非誤稱通過，邏輯正確。

## 3. 邊界情況

- 判定：部分通過
- 證據：
  - prompt 以「。」、「，」、「？」具體列出全形標點，並禁止英文半形替換；也禁止 placeholder 與要求無修改時保留原文。
  - native schema 仍鎖定 cue 數量 1/1、id/text/reason 與 additionalProperties false；實測沒有 malformed cue 結構。
  - 未覆蓋多 cue、混合中英文／數字標點、括號／引號、翻譯模式及模型在不同溫度／上下文長度下的標點一致性；品質仍需人工逐段確認。

## 4. 程式碼品質

- 判定：通過
- 證據：
  - production diff 僅在既有 system prompt 加入明確規則，未重複實作 parser 或繞過 provider contract。
  - `npm test`／`npm run check` 通過，未發現 prompt 字串破壞語法、schema 或既有 AI 任務流程。
  - 文件同時記錄「流程 completed、JSON／cue contract 正常」及「標點差異仍須人工品質確認」，沒有以任務完成狀態掩蓋內容風險。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：
  - 2026-07-29 執行 `npm test && npm run check`：治理、語法、provider、optimizer、UI、core 完整通過。
  - 真實 Ollama native `/api/chat` 重跑成功，回應 JSON：`{"cues":[{"id":1,"text":"這是一個測試字幕.","reason":""}]}`；結構與 cue count 通過，中文全形標點品質仍失敗。
  - 沒有自動化 assertion 檢查標點 preservation（模型品質不宜用固定單次輸出作硬性測試）；目前以 evidence／人工風險揭露替代，合理但不足以宣稱品質完成。
  - LM Studio structured output 先前真實 HTTP 400、斷網及跨平台驗收仍未覆蓋。

## 6. 實際運行結果

- 判定：部分通過
- 證據：
  - Ollama 實際 response 為合法 single-cue JSON、id 1、無額外 cue、`reason` 空字串；任務契約可接受。
  - 同一 prompt 明確要求全形標點後，模型仍輸出英文句點；因此完成狀態只代表結構／流程成功，不代表字幕內容品質成功。
  - changelog／測試稽核仍把此結果列為人工品質風險，並保留 LM Studio、斷網與 0.48 未發布限制。

## 綜合判定

- 結論：有條件通過
- 阻擋問題（若有）：無本輪新的安全或結構性阻擋；全形標點未改善是已揭露的模型品質限制，不是未處理的誠信問題。
- 剩餘風險：
  - `llama3.2:1b` 即使收到明確全形標點 prompt，仍會將「。」轉為「.」；需人工接受或更換模型／提示策略。
  - 標點 preservation 未有可普遍化的自動品質門檻；多 cue、其他標點及多語模式尚未驗證。
  - LM Studio structured output、真正斷網、跨平台完整驗收仍未完成。
- 給主要開發代理的具體要求：
  1. 保留本輪 prompt 與「實測仍為英文句點」的風險記錄，不宣稱品質修正已成功。
  2. 可補一個人工／evidence checklist，記錄模型輸出與接受／拒絕決定；不以固定模型輸出硬塞自動通過。
  3. 完成 LM Studio 支援模型及真正斷網端到端前，0.48 維持開發中。

**本輪「2026-07-29 — 本機 LLM 結構化輸出修正」round10 獨立複審結論為有條件通過：全形標點 preservation prompt 已加入既有 native structured-output 與 strict cue contract 流程，`npm test`／`npm run check` 通過；實際 `llama3.2:1b` 重跑回合法 single-cue JSON、cue count 1/1、ID 正確，但即使明確要求全形標點仍將「。」輸出為英文「.」，文件已如實列為人工品質風險且未放寬驗證；本輪無新安全阻擋，但 LM Studio structured output、真正斷網及跨平台驗收仍未完成，0.48 不得宣稱完成或發布就緒。**

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、測試、HTTP 查詢與驗證，未修改任何其他專案來源或治理檔案。
- 若上述聲明不實，本報告無效。
