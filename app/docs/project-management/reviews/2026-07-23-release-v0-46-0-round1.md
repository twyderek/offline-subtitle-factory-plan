# 獨立審查報告：0.46.0 正式打包與發布

- 審查對象 commit／版本：`433cafb` / `0.46.0`，分支 `codex/release-v0.46.0`
- 對應 08-CHANGE-LOG 條目：2026-07-23 — 0.46.0 正式打包與發布
- 審查輪次：round1
- 審查代理啟動時間、上下文來源：2026-07-23（Asia/Taipei）；由獨立審查上下文啟動，未沿用主要開發代理對話記憶。

## 1. 需求完整性

- 判定：部分通過
- 證據：`package.json` 版本為 `0.46.0`；`.github/workflows/windows-preview.yml:1-9,97-104` 已針對 0.46.0 分支／標籤與 Windows artifact 命名；`RELEASE-NOTES-0.46.0.md:5-17` 揭露雙語功能與尚未覆蓋的驗證項目。發布授權已記錄於 `docs/project-management/08-CHANGE-LOG.md:64-68`，且明確接受未簽章、ad-hoc 未公證及部分未覆蓋風險。
- 發布流程仍未完成 GitHub `v0.46.0` Release；目前狀態文件 `docs/project-management/00-CURRENT-STATUS.md:50-52` 仍寫發布候選，故不能判定正式發布需求已完整完成。

## 2. 邏輯正確性

- 判定：部分通過
- 證據：2026-07-23 執行受控環境 `npm run check` 通過，包含雙語資料模型、AI provider contract、UI 與核心回歸。`package.json:47-74` 使用來源白名單並加入 env／憑證／金鑰／機密目錄排除；Windows workflow `:54-95` 驗證 EXE 數量、7z archive、手冊檔與 SHA-256。
- `package.json:102-134,158-188` 的 `extraResources` 僅指定離線 runtime、模型、manifest 與 0.45.2 手冊資源，未指向 env、credentials 或 secrets。`asar: false` 使內容可直接檢查，這對發布前稽核有利但提高誤放檔案的風險。
- macOS ZIP 是在 electron-builder ZIP 產物失敗後以 `ditto` 重新建立；其 SHA-256 已存在於 `RELEASE-SHA256-0.46.0-macos-arm64.txt`，但沒有本輪可回溯的完整 updater metadata／blockmap 一致性驗證，因此 macOS 自動更新邏輯仍有條件。

## 3. 邊界情況

- 判定：部分通過
- 證據：`npm run check` 的輸出明確通過雙語舊單語遷移、排列、SRT/VTT 邊界、時間碼／空文字、cue ID 與核心取消／分頁等測試；`git ls-files` 的敏感檔名掃描未回傳 env、私鑰、憑證、token 或 password 檔案；常見 token／私鑰內容模式掃描亦無命中（輸出已刻意不顯示內容）。
- 未覆蓋邊界：Windows 乾淨機安裝／解除安裝／SmartScreen、macOS Gatekeeper／公證、Electron packaged renderer、真實外部 LLM provider、AI 雙語 response contract 與 macOS updater 下載／差分更新；這些風險在 `RELEASE-NOTES-0.46.0.md:13-17` 有揭露。

## 4. 程式碼品質

- 判定：通過（發布工程除外項目另列）
- 證據：`node --check` 包含 `server.mjs`、`public/app.js`、`public/review.js`、`public/trim.js` 與 `electron/main.mjs` 均由 `npm run check` 執行通過；`git diff --check` 通過；工作樹在審查開始時為 clean。發布設定、機密排除與資產命名集中於 `package.json`、`.gitignore` 與 workflow，具可追溯性。
- 注意：workflow 的步驟名稱仍稱「Build signed NSIS Setup and Portable packages」，但缺少 secrets 時會依 `:47-52` 建立 unsigned internal preview；這不影響條件判定，但容易讓讀者誤解，正式發布前應改成不暗示必然簽章的名稱或在結果中明示。

## 5. 測試覆蓋

- 判定：部分通過
- 證據：2026-07-23 受控環境執行 `npm run check` 通過；輸出包含治理文件驗證、媒體編輯、雙語字幕、AI optimizer、AI provider、review UI 與核心回歸。GitHub Actions run `29977780141` 成功，Windows job `89113121005` 的來源／真實 FFmpeg regression、unsigned build、archive／SHA 驗證與 artifact upload 均成功；簽章步驟因無 `CSC_LINK` 被跳過。
- 本地未提升權限的同一測試因 sandbox 禁止 listen `127.0.0.1:21592` 而失敗；在受控環境重跑後通過，故記為環境限制而非程式失敗。
- Windows artifact metadata 顯示 `offline-subtitle-factory-0.46.0-windows-x64`、約 433 MB、未過期；本輪未完成其內容下載後的逐檔交叉檢查，僅有 CI 內部檢查證據。

## 6. 實際運行結果

- 判定：部分通過
- 證據：macOS 候選 `../dist/offline-subtitle-factory-0.46.0-macos-arm64.dmg` 與 ZIP 均存在；已知 SHA-256 分別為 `21e54b6906c871146ffc6b2587268d07144f9a6cf54490cd1cb1a228453026d0` 與 `818c609fd1a7288af00129f381ce65f75cf44759c81fbcc5d808152db6b7d78c`，且與 `RELEASE-SHA256-0.46.0-macos-arm64.txt` 一致。既有驗證證據記載 DMG `hdiutil verify`、ZIP `unzip -t` 通過；macOS app codesign 為 ad-hoc、TeamIdentifier 未設定。
- Windows CI run `29977780141` 成功建立並上傳 unsigned preview artifact，且實際執行真實 FFmpeg regression 與 EXE archive／SHA 驗證。沒有 Windows Authenticode，也沒有跨平台乾淨實機操作證據。
- `../dist` 仍可見由前次失敗建置留下的 macOS `.blockmap` 檔；不可在未重新證明其對應 ZIP／metadata 前上傳。發布時應只選取已核對的資產，或重新建立一致的 updater 資產組。

## 機密與發布面額外核對

- 判定：通過（目前候選未發現機密）；封裝交叉驗證仍需完成
- 證據：Git tracked sensitive-name scan 未命中 `.env*`、`*.pem`、`*.p12`、`*.pfx`、`*.key`、secrets／credentials 等；常見 `sk-`、Google API key、GitHub token、PRIVATE KEY 模式掃描未命中。`.gitignore:19-37` 已涵蓋 env、私鑰／憑證副檔名與機密目錄。`package.json:47-74` 的 build files 白名單與負項排除未包含機密來源，平台 `extraResources` 亦未指定機密目錄。
- 未能以完整下載後的 Windows artifact 做逐檔內容稽核；因此發布前仍應從 artifact 解開檢查安裝包／portable 內沒有 `.env`、憑證、私鑰、password 或 token 檔案。任何命中都應停止發布。

## 綜合判定

- 結論：有條件通過
- 阻擋問題（若有）：
  1. 公開 GitHub `v0.46.0` Release 已建立，但 Windows artifact 仍受限於審查代理端逐檔下載／交叉檢查尚未完成。
  2. macOS updater metadata／blockmap 與手動重建 ZIP 的一致性尚未驗證。
  3. Windows 未簽章、macOS 未公證、Windows／macOS 實機與 Electron renderer smoke test 尚未完成；需求方已於 `docs/project-management/08-CHANGE-LOG.md:66-68` 明確接受這些發布風險，但接受風險不等於驗證已完成。
- 可逐字引用的完整結論句：**本輪獨立發布審查結論為有條件通過：公開 v0.46.0 Release 已建立，但 Windows artifact 尚未完成審查代理端逐檔交叉檢查、macOS updater metadata／blockmap 尚未驗證；需求方已接受 Windows 未簽章、macOS 未公證、Windows／macOS 實機與 Electron smoke test 尚未完成的發布風險，且未核對資產不得上傳。**
- 剩餘風險：Windows 資產為 unsigned preview；macOS 為 ad-hoc、未 Developer ID／公證；未完成 Windows／macOS 乾淨實機與 Electron renderer smoke test；AI 雙語 response contract、真實外部 provider 與 macOS updater 差分更新仍未覆蓋；GitHub Actions 有 Node 20 deprecation annotation。
- 給主要開發代理的具體修正要求：發布前完成 Windows artifact 逐檔／機密內容核對，決定並核對 macOS updater metadata 與 blockmap（或明確不發布 updater metadata）；只上傳已核對資產與 checksum；建立公開 Release 後重新核對 asset 名稱、大小、digest、下載 URL 與 Release notes，並將結果回寫工作紀錄與狀態文件。

## 審查代理聲明

- 本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案。
- 若上述聲明不實，本報告無效。
